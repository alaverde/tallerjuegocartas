
export const getSuiteValue = (suite) =>{
    if(suite === "CLUBS")
        return 1;
    if(suite === "DIAMONDS")
        return 2;
    if(suite === "SPADES")
        return 3;
    if(suite === "HEARTS")
        return 4;
}